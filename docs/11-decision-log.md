# Decision Log

This file records project decisions that affect scope, architecture, and implementation.

| ID      | Date       | Decision                                                                                       | Status   | Rationale                                                                                                                                                                                                                                                                            | Consequences                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| ------- | ---------- | ---------------------------------------------------------------------------------------------- | -------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| DEC-001 | 2026-04-28 | Use Expo because the prototype already exists in Expo Go.                                      | Accepted | The current project is already an Expo app, making iteration and testing faster for MVP/thesis work.                                                                                                                                                                                 | Continue with Expo Router and Expo-compatible libraries.                                                                                                                                                                                                                                                                                                                                                                                            |
| DEC-002 | 2026-04-28 | Use TypeScript for maintainability.                                                            | Accepted | TypeScript helps keep service contracts, data models, and screen props safer as the app grows.                                                                                                                                                                                       | New app code should be written in TypeScript with shared types in `/types`.                                                                                                                                                                                                                                                                                                                                                                         |
| DEC-003 | 2026-04-28 | Use Supabase for MVP backend speed.                                                            | Accepted | Supabase provides Auth, PostgreSQL, Storage, and Row Level Security quickly enough for MVP delivery.                                                                                                                                                                                 | Backend calls should be isolated in `/services` so Supabase can be replaced later if needed.                                                                                                                                                                                                                                                                                                                                                        |
| DEC-004 | 2026-04-28 | Use PostgreSQL because the app has relational data.                                            | Accepted | Users, profiles, services, jobs, conversations, verification, and reviews are relational and need constraints.                                                                                                                                                                       | Use SQL migrations, foreign keys, indexes, and RLS policies.                                                                                                                                                                                                                                                                                                                                                                                        |
| DEC-005 | 2026-04-28 | Keep payments outside the app.                                                                 | Accepted | Payments add legal, technical, and operational complexity beyond MVP/thesis scope.                                                                                                                                                                                                   | Job budgets are informational only. Final payment and agreements happen outside Konektado.                                                                                                                                                                                                                                                                                                                                                          |
| DEC-006 | 2026-04-28 | Keep architecture migration-friendly for possible future self-hosting.                         | Accepted | The project may later move away from Supabase to a home server or custom backend.                                                                                                                                                                                                    | Keep screens thin, isolate backend logic in `/services`, and avoid Supabase-specific logic in UI components.                                                                                                                                                                                                                                                                                                                                        |
| DEC-007 | 2026-04-28 | Use lightweight onboarding with unverified viewer mode before full verification.               | Accepted | New users should understand and enter the app quickly, while barangay verification remains the trust gate for marketplace interaction.                                                                                                                                               | Initial onboarding stays simple. Verification collects heavier requirements such as contact confirmation, email, optional phone number, ID, services, credentials, and supporting details. Unverified users can browse but cannot post, message, save if gated, or review.                                                                                                                                                                          |
| DEC-008 | 2026-04-28 | Prefer phone-first account entry, with optional email collected during verification.           | Replaced | Mobile number entry is more familiar locally, but SMS provider setup and Android phone testing are not currently available.                                                                                                                                                          | Replaced for MVP by DEC-014. Keep phone-first auth as a future option, not current MVP scope.                                                                                                                                                                                                                                                                                                                                                       |
| DEC-009 | 2026-05-01 | Use Home, Post, Messages, Profile as the final mobile navigation.                              | Replaced | The current Figma flow is built around discovery, listing creation/management, coordination, and profile management.                                                                                                                                                                 | Replaced by DEC-045, which promotes Search to a main bottom-navigation destination.                                                                                                                                                                                                                                                                                                                                                                  |
| DEC-010 | 2026-05-01 | Use message-based job interest instead of a formal application flow for MVP.                   | Accepted | Konektado behaves more like a local barangay marketplace than Upwork. Low-literacy users will understand messaging and hiring more easily than applications.                                                                                                                         | Remove Apply/Application from primary UI. Implement conversations, messages, and Mark Hired. Existing `job_applications` code becomes legacy/prototype code.                                                                                                                                                                                                                                                                                        |
| DEC-011 | 2026-05-01 | Use Services as the user-facing provider profile concept.                                      | Accepted | Services is clearer than Skills for local users and matches the final profile/post designs.                                                                                                                                                                                          | UI should say Services. Data can temporarily map from `provider_profiles.service_type` until a services table exists.                                                                                                                                                                                                                                                                                                                               |
| DEC-012 | 2026-05-01 | One account has both Work Profile and Hiring Profile.                                          | Accepted | Users can both offer services and hire help without switching accounts.                                                                                                                                                                                                              | Profile tab needs two modes. Shared identity stays in `profiles`; role-specific data lives in provider/client profile data.                                                                                                                                                                                                                                                                                                                         |
| DEC-013 | 2026-05-01 | Public feed/search cards should not repeat verified badges by default.                         | Accepted | Verification is already the gate for interaction, and repeated badges clutter the feed.                                                                                                                                                                                              | Keep verification cues in profile, verification flow, details, and owner/admin states.                                                                                                                                                                                                                                                                                                                                                              |
| DEC-014 | 2026-05-02 | Use email OTP signup plus password login for the MVP and defer custom SMTP and SMS/mobile OTP. | Accepted | The current Figma signup flow uses email code verification, while SMS provider setup and Android phone testing are not currently available. Supabase can provide default auth emails without custom SMTP for MVP/demo work.                                                          | MVP signup uses email OTP, then password creation. Login uses email/password. Configure the Supabase Auth Magic Link and Confirm sign up templates to include `{{ .Token }}` so the app receives a code instead of a link. Do not buy or integrate an SMS provider or require custom SMTP for the MVP. Phone-first auth can be revisited after the thesis/demo path is stable.                                                                      |
| DEC-015 | 2026-05-02 | Use passwordless email OTP before password creation for onboarding.                            | Accepted | The ideal Konektado onboarding path is email -> OTP -> session -> create password, and Supabase's supported way to send an OTP before a password exists is `signInWithOtp()` with an email template containing `{{ .Token }}`.                                                       | The active onboarding auth flow uses `signInWithOtp({ shouldCreateUser: true })`, `verifyOtp({ type: 'email' })`, then `updateUser({ password })`. Magic Link and Confirm sign up templates should both show the same six-box `{{ .Token }}` body. Supabase Auth OTP length must be configured to 6 digits.                                                                                                                                         |
| DEC-016 | 2026-05-03 | Add a Supabase onboarding schema migration before expanding marketplace features.              | Accepted | The live app reached the password/role handoff but failed because `public.profiles` did not exist in Supabase. The fastest stable path is to provision the tables the current onboarding code already writes before building the wider marketplace schema.                           | `supabase/migrations/20260503001433_initial_app_schema.sql` is the active database setup baseline. It creates owner-scoped RLS tables for profiles, roles, provider/client onboarding details, verification uploads, jobs, and the `verification-files` storage bucket. Future work can migrate legacy `verifications` naming toward documented `verification_requests`.                                                                            |
| DEC-017 | 2026-05-03 | Use a basic viewer entry with taste setup before verified full benefits.                       | Accepted | Users should enter quickly like a basic account, understand nearby jobs/workers, and only complete heavier barangay requirements when they want to interact.                                                                                                                         | Onboarding supports provider, client, and both intent. It stores offered/needed services in `user_preferences`, seeds `provider_profiles.service_type` for provider/both users, and sends unverified users to a verified-content viewer feed. Barangay verification remains required for posting, messaging, saving if gated, reviews, and public interactable marketplace presence.                                                                |
| DEC-018 | 2026-05-03 | Use onboarding intent to set the first Home feed filter.                                       | Accepted | The first feed should immediately match why the user joined without adding database ranking complexity in this slice.                                                                                                                                                                | Provider intent opens Home on Jobs, client intent opens Home on Workers, and both or missing intent opens Home on For you. Home remains mostly demo/static for now; preferences only affect ordering and filter defaults until verified-origin database filtering is built.                                                                                                                                                                         |
| DEC-019 | 2026-05-03 | Implement Dashboard/Home as a Figma-matched demo feed before database ranking.                 | Accepted | The dashboard is the next user-facing surface after onboarding and should show the final marketplace direction before full job/profile detail, messaging, posting, and verification flows are wired.                                                                                 | `app/(tabs)/index.tsx` now has the Konektado wordmark header, notification button, search bar, verification/setup prompt, For you/Jobs/Workers filters, and a mixed jobs/workers feed using reusable `JobCard` and `WorkerCard` components. Feed data remains static/demo in `constants/demo-data.ts`. Message, Save, and post actions stay verification-gated. The initial slice used browse-only prompts before DEC-022 added static details. |
| DEC-020 | 2026-05-03 | Use Satoshi as the only app font.                                                              | Accepted | Konektado's visual language should be consistent across onboarding, dashboard, and shared text components.                                                                                                                                                                           | `AvantGarde` was removed from font loading and remaining references were changed to Satoshi. Future UI work should use Satoshi font families only.                                                                                                                                                                                                                                                                                                  |
| DEC-021 | 2026-05-03 | Remove "Both" from role selection; capture initial intent, not final capability.               | Accepted | DEC-012 already supports both profiles in one account. Asking users to declare both roles at signup conflicts with lightweight onboarding and confuses intent (why user joined) with capability (what they can do). Users naturally add a second profile when needed, not at signup. | Onboarding now shows only 2 role options: "I want to find work" (provider) and "I want to hire someone" (client). Users enter one role at signup and can add a second profile from Profile tab later. Job services collection now collects only offered services for providers or needed services for clients—no dual collection. First Home feed still respects initial intent (Jobs for providers, Workers for clients).                          |

| DEC-022 | 2026-05-03 | Add static detail routes before database-backed marketplace data.                              | Superseded | Users need to inspect jobs and workers from Home while remaining in unverified viewer mode, and the app needs stable routing before real search/ranking work.                                                                                                                        | View Job opened `app/job/[jobId].tsx`; the original View Profile route was later superseded by the selected service offer route in DEC-041. Locked Message, Save, and Post actions route to `app/verification.tsx`; DEC-023 later connected that route to a pending-request flow.                                                                                                                                                                                    |
| DEC-023 | 2026-05-03 | Connect the Figma verification intro to a live pending-request flow.                           | Accepted | Verification is the core trust gate for posting, messaging, saving, and reviews, so users need a real submission path before admin review is built.                                                                                                                                   | `app/verification.tsx` now uses the established Konektado Figma intro and a Figma-style multi-step request form. `services/verification.service.ts` loads profile/preferences, updates contact details, uploads files to the `verification-files` bucket, writes `verification_files`, and creates a pending row in the current `verifications` table. Admin approval/rejection remains pending.                                                         |
| DEC-024 | 2026-05-03 | Replicate the established Figma verification storyboard instead of using an improvised form.    | Accepted | The verification flow already has a complete Konektado Figma design, and it is the best visual source of truth for this trust-critical path.                                                                                                                                          | Verification follows the Figma sequence: intro, preflight, account details, contact-code UI, ID type, document capture, face-photo guidance/capture, review, submit, pending, approved, and correction states. MVP wording still follows DEC-010, so "Apply" is replaced with message/job-interest language where needed.                                                              |
| DEC-025 | 2026-05-03 | Add the functional marketplace MVP database and service layer while keeping current `verifications` naming. | Accepted | The thesis/demo workflow needs real jobs, services, conversations, admin approval, hiring, and reviews before advanced feed ranking or polish. Renaming verification tables would add migration risk during the MVP slice. | `supabase/migrations/20260503023000_marketplace_mvp.sql` adds `services`, `conversations`, `messages`, `saved_items`, and `reviews`, extends `jobs`, and adds RLS/admin policies. Screens now call `/services` for jobs, service profiles, conversations, admin verification review, and reviews. A later cleanup can rename `verifications` to `verification_requests`. |
| DEC-026 | 2026-05-03 | Use fixed Supabase seed accounts for end-to-end MVP testing. | Accepted | The MVP needs repeatable test data for verification gates, admin approval, posting, service search, messaging, hiring, and reviews without manually creating accounts each run. | `supabase/seed.sql` creates six `@konektado.test` auth users with password `Test12345!` plus profiles, roles, preferences, services, jobs, conversations, messages, verification requests/files, saved items, and reviews. The seed deletes and recreates only fixed demo UUIDs so it can be rerun for demos/tests. |
| DEC-027 | 2026-05-03 | Use one shared static marketplace demo source and a two-variant worker profile detail flow. | Superseded | Home, Search, and detail routes were defining overlapping demo jobs/workers, which caused ID drift and made the worker detail screen diverge from the current Figma worker-profile nodes. | `constants/marketplace-demo-data.ts` became the single static source for demo jobs, workers, worker-detail content, and work-history cards. The selected service detail route was later renamed to `/services/[serviceId]` in DEC-041. |
| DEC-041 | 2026-05-06 | Separate service detail route semantics from future worker profiles. | Accepted | The current worker detail screen loads a selected service offer by service id, while a person-level worker profile is a different future product surface. | `app/services/[serviceId].tsx` is the route for selected service offers and keeps the current detail UI. `/workers/[workerId]` is reserved for a future general worker profile and should not be linked until that screen exists. |
| DEC-028 | 2026-05-03 | Complete verification end to end before expanding Post or Messages. | Accepted | Barangay verification is the trust gate for posting, messaging, saving, and reviews. The thesis/demo path needs admin approval/rejection and post-approval unlock to work before additional marketplace surfaces are expanded. | The next implementation slice finishes admin verification review, rejection correction, profile verification timestamp updates, admin seeded-account routing, and unlock refresh. Keep current `verifications` and `verification_files.verification_id` naming for this slice; defer any rename to `verification_requests` until after the demo-critical flow is stable. |
| DEC-029 | 2026-05-03 | Move next to verified job posting; treat camera capture as verification polish. | Accepted | Verification can now be submitted, reviewed, approved/rejected, and used to unlock gated actions. The next proof point should be the first real verified marketplace action rather than another broad UI or capture-library pass. | The next slice makes Post create real jobs end to end for approved users. Camera capture for ID/selfie can be added as a small verification hardening task using Expo-compatible camera/image-picker APIs, but it should not block verified Post unless the demo specifically requires live camera capture. |
| DEC-030 | 2026-05-04 | Implement the Figma Post flow as verified job posting only; keep service-post builder nodes reference-only. | Accepted | The supplied Figma nodes include job and service post screens, but this implementation slice is explicitly focused on verified job creation and preserving existing marketplace flows. | Post uses the Figma dashboard, post-type sheet, job builder, preview, active posts, and renew-state visuals for job posting. Service creation remains on the existing service route. Job photos, renewal business rules, auto-reply execution, hiring, reviews, ranking, advanced search, and Apply/Application wording remain out of scope. |
| DEC-031 | 2026-05-04 | Let unverified users compose private job drafts, but keep publishing verification-gated. | Accepted | The Figma verification gate supports a smoother path: users can finish a draft, then understand why barangay verification is required at publish time. This reduces dead-end friction without weakening marketplace trust. | Add `job_drafts` for owner-private draft persistence. Unverified users can create/edit drafts and see the publish gate. Only verified users can create public `jobs` rows. Drafts never appear in Home, Search, Job Detail, conversations, or provider browsing. |
| DEC-032 | 2026-05-04 | Split job post category into Job Category and Service Needed. | Accepted | The latest Figma job-post flow separates broad category from the specific service, which makes job posts easier to filter and keeps context tags from becoming service names. | `jobs.service_needed` and `job_drafts.service_needed` store the selected service. The New Job Post form uses category-dependent service options, clears service/tags when category changes, and keeps tags as optional context descriptors. |
| DEC-033 | 2026-05-04 | Make Add Photos functional for job posts using public Supabase Storage. | Accepted | The post builder already had a visual Add Photos block. Wiring it now gives the job flow a real photo upload path without changing the publish gate or navigation. | Job photos upload to the `job-photos` bucket, store as `photo_urls` on `jobs` and `job_drafts`, and show in the post preview and job detail screen. |
| DEC-034 | 2026-05-04 | Send barangay verification emails from backend transactional templates instead of Supabase Auth templates. | Accepted | Verification updates are part of the resident trust workflow, not auth. They need branded transactional emails that track status changes without touching the Supabase Auth OTP templates. | Add custom HTML templates for submitted, approved, needs-more-info, and rejected verification states under the backend edge function. The sender uses the same configured relay or SMTP transport as the project email backend, while uploaded IDs and documents remain private and are never attached. |
| DEC-035 | 2026-05-04 | Prefer a domain-backed transactional relay/API for custom verification emails; keep SMTP only as fallback. | Accepted | The project now owns `konektado.app` and can use a proper sending domain for branded transactional mail. Edge-function email delivery is more reliable and observable through a provider relay/API than through ad hoc mailbox SMTP. | Supabase Auth OTP emails remain configured in Supabase Auth templates and can continue using Supabase's auth sender path. Custom verification status emails should move to the Resend Email API on `konektado.app`, with SMTP kept only for temporary testing or emergency fallback. |
| DEC-036 | 2026-05-04 | Use a hybrid selector for service-post category choice: bottom sheet as canonical selector, inline pills as quick picks. | Accepted | The service taxonomy is expected to grow beyond the visible chip row, and the job-post builder already established a stronger searchable bottom-sheet pattern for canonical selection. Quick-pick pills still help common choices feel fast and familiar. | Service posts now use single-select category choice. The chosen service is shown in one clear field, the bottom sheet is the source of truth for the full list, and inline pills are shortcuts only rather than multi-select state. |
| DEC-037 | 2026-05-04 | Route service posting through a preview-before-publish screen, aligned with the job-post flow. | Accepted | The Figma service-post storyboard includes a dedicated review step, and the job-post flow already proved that preview-before-publish helps catch mistakes and explain the public visibility rules before a listing goes live. | `create-service` now sends users to a service preview screen before publish. The preview reuses the marketplace card language and shows publication safety reminders plus listing-option readouts. Service publishing still writes only the current `services` schema fields; listing-option persistence can be added later when the backend supports it. |
| DEC-038 | 2026-05-04 | Reuse a shared map preview and keep service location selectable through a single-choice barangay sheet. | Accepted | The service builder should match the job-post location treatment instead of using plain text only, and the MVP location choice is intentionally narrow. Reusing the map preview keeps the visual language consistent while avoiding a separate location system. | Service and job post screens now share a map preview component. The service builder opens a bottom sheet for the public location choice, but the only MVP selection is Barangay San Pedro. |
| DEC-039 | 2026-05-04 | Use a list-only barangay picker for both job and service builders. | Accepted | The previous picker copied category UI patterns that made location feel noisy and vague. A plain list is the correct interaction for a single public barangay choice. | Both builders now use the same reusable barangay sheet with search and rows only. Category pills and category labels are removed from the location flow, and the screen copy uses "Barangay" instead of the vague "Location". |
| DEC-040 | 2026-05-04 | Make service-post tags explicit and remove redundant quick-pick service pills from the builder. | Accepted | The chevron service field already communicates that the full category picker exists, so inline service pills and a "More services" pill duplicate the same action. Tags are still useful, but they should be separate multi-select metadata used by Home cards and service details. | The service builder now has one canonical service selector, category-specific service tags, service-photo upload, barangay, and listing options. The `services` model gains tags, photo URLs, public barangay fields, and listing-option flags so published service posts can carry the same public card data the marketplace surfaces need. |
| DEC-041 | 2026-05-05 | Treat current marketplace card labels and `For you` ordering as placeholder behavior that must be corrected before final polish. | Accepted | The existing feed can browse real or seeded content, but labels such as `Posted in Konektado`, `New worker`, and `Jobs done pending` do not reflect meaningful marketplace trust signals. The `For you` feed also appears type-grouped instead of relevance-driven, which conflicts with the intended mixed discovery model. | Feed and card work should prioritize meaningful trust and relevance metadata such as ratings, review counts, jobs done, location, and freshness. `For you` should be scored and mixed rather than visibly grouped by jobs first then workers. Messaging remains the primary coordination flow, so related UI fixes should be treated as product-critical rather than optional polish. |
| DEC-042 | 2026-05-05 | Implement Messages as a Figma-aligned text-only coordination flow for jobs and services. | Accepted | Konektado's MVP uses messaging and Mark Hired instead of applications, so chat must be a clear product surface rather than a placeholder. The Figma message set defines inbox filters, request grouping, job/service context, quick prompts, details, report, and delete states. | Messages now supports job and service conversations, a lightweight inbox, conversation context cards, quick replies, Mark Hired for job clients, reported status, and archived chats. Attachments, calls, read receipts, push notifications, group chat, and full report moderation remain future work. |
| DEC-043 | 2026-05-05 | Use a unified Home feed card for job and service posts, while keeping specialized cards in Search. | Accepted | The Home dashboard mixes job requests and worker/service offers. Separate card designs make the mixed feed feel inconsistent, but Search still benefits from task-first job cards and person-first worker cards because users are in a focused browsing mode. | Home For you, Jobs, and Workers filters render through one shared feed card shell with consistent avatar, spacing, typography, metadata, and tags. Post type is communicated through plain labels and intent titles such as "Posted a job", "Posted a service", "Looking for...", "Need help with...", and "I offer...". Search result cards remain specialized. |
| DEC-044 | 2026-05-05 | Treat Home feed cards as tappable community posts instead of CTA-heavy listings. | Accepted | Konektado's Home feed should feel closer to a local social/community feed than a search-results list. Users can tap the card to inspect it, and large repeated "View Job" / "View Profile" buttons make the mixed feed feel generic and transactional. The Konektado yellow dot is the online-presence motif tied to the brand mark. | Home feed cards no longer show a full-width primary CTA button. The whole card remains tappable, the save icon remains explicit, tags are capped with overflow count, and role-specific trust metadata distinguishes job and service posts without changing the shared card shell. |
| DEC-045 | 2026-05-05 | Promote Search to the bottom navigation between Home and Post. | Accepted | Search is a P0 marketplace behavior and should be discoverable as intentional job/service discovery, not only a Home entry point. Home should stay a passive/community feed with lightweight filter pills, while Search owns text search, category browsing, and specialized result cards. | Main tabs are Home, Search, Post, Messages, Profile. The Home header no longer carries the large search bar. Search remains a focused destination with the prominent search input and job/worker result modes. |
| DEC-046 | 2026-05-05 | Keep the Konektado brand header on Home only and use screen-specific headers on other main tabs. | Refined | Bottom-navigation tabs should clearly orient users to the current task. Repeating the brand wordmark on Search, Messages, Post, and Profile weakens page identity and consumes header space that should belong to the tab's own tools and actions. | Home keeps the Konektado wordmark/header. Messages keeps a Messages title header, and future secondary tab headers should follow the same task-specific pattern. Search was later refined by DEC-047 to use the search module itself as the header. |
| DEC-047 | 2026-05-05 | Use the search field as the Search tab header instead of a repeated Search title. | Accepted | The active bottom navigation state plus the search input already identify the Search tab. A large "Search" title repeats information and pushes marketplace discovery content lower on the screen. | Search starts with a safe-area-aware search module containing the search input and Find Jobs / Find Workers segmented control. The two controls share one parent width and alignment. Messages, Post, and Profile can continue using screen-specific title headers. |
| DEC-048 | 2026-05-05 | Make Conversation Details a dedicated full-screen route instead of a modal over chat. | Accepted | The Details surface contains navigation, job/service context, support actions, and destructive actions, so it should read as a normal page with full viewport ownership. Keeping it inside the chat route makes the composer and underlying conversation feel visually present even when hidden. | Conversation detail actions now route to `/conversation/[conversationId]/details`. The Details page fetches its own conversation data, uses a full white screen with app bar and scroll content, and keeps report/delete as secondary confirmation surfaces. |
| DEC-049 | 2026-05-05 | Make chat conversation-first while keeping job/service context compact and always accessible. | Accepted | Marketplace context matters in Konektado chats, but a large workflow-style card makes the thread feel less natural than common messaging apps. Users should see the job/service quickly without sacrificing message density or keyboard reliability. | Conversation headers are compact, job/service context is a fixed expandable strip under the app bar, consecutive messages are grouped tightly, timestamps are hidden until the bubble is tapped, and Android keyboard layout uses resize so the composer can stay visible while typing. |
| DEC-050 | 2026-05-06 | Expand the MVP taxonomy to selected home, digital, and tech support services without changing schema. | Accepted | Physical-only services are too narrow for student and Gen Z adoption, but a broad online freelancing scope would add research, moderation, and defense risk. | Use a taxonomy-only MVP with `Home & Local Help`, `Learning & Digital Help`, and `Tech & Document Support`. Keep existing string fields for `jobs.category`, `jobs.service_needed`, `services.category`, and onboarding preferences. Exclude gaming, licensed/high-risk work, academic cheating, account sharing, regulated advice, file-delivery workflows, and professional certification. Defer structured category tables, service type, location-required, and risk-level fields. |
| DEC-051 | 2026-05-06 | Keep hosted demo taxonomy data in sync with the local MVP seed through a repeatable refresh script. | Accepted | The app points at a hosted Supabase project, so local-only seed changes are not enough to keep the cloud demo aligned for reviews or second-machine testing. | `npm run refresh:cloud-demo-taxonomy` updates the hosted demo rows without changing schema. Local development still uses `supabase db reset` and `supabase/seed.sql`; the cloud refresh is a separate maintenance step for demo parity. |
| DEC-052 | 2026-05-06 | Treat auth-session transitions as blocking for onboarding route evaluation. | Accepted | A logged-in user must not briefly render onboarding while the shared profile cache is still hydrating after sign-in or app restore. The onboarding name step flashing after login is a product-level bug, not an acceptable loading state. | The shared profile cache raises a loading state during auth identity transitions, and the onboarding stack redirects authenticated users who no longer need onboarding before rendering its screens. Existing onboarding users still enter the same flow, but already-onboarded users stay on auth splash until route status is settled. |
| DEC-053 | 2026-05-06 | Make Search discovery use Konektado primary tokens and guide filter selection forward. | Accepted | The taxonomy/filter structure was correct, but the screen still leaned on generic dark pills and the verification accent, which made Search feel visually detached from the main Konektado brand. The filter sheet also benefited from a small guided-flow improvement when choosing a service group. | Search pills, segmented control, search affordances, and filter actions now use the primary brand token and primary-soft surfaces instead of ad hoc black emphasis. In the filter sheet, selecting a service group auto-scrolls to the Specific service section so the next constrained choice is immediately visible. |
| DEC-054 | 2026-05-06 | Make Search service groups depend on selected work type and reset invalid filter groups to All groups. | Accepted | Physical/on-site and digital/remote search intent should narrow the taxonomy before users choose a group or service. When a previous group becomes invalid after changing work type, resetting to All groups is safer than silently selecting a specific group because it avoids unexpected over-narrowing. | The UI labels the unrestricted work type as All while keeping the internal `either` value for now. Physical work type shows physical plus either-capable services, digital work type shows digital plus either-capable services, and All shows the full MVP taxonomy without database schema changes. |
| DEC-055 | 2026-05-08 | Use Supabase recovery OTP plus in-app password reset for Forgot Password. | Accepted | Konektado already uses email-code auth, so password recovery should match the app's six-box OTP flow instead of relying on a link-only email or deep-link setup. | Forgot Password calls `resetPasswordForEmail`, verifies `verifyOtp({ type: 'recovery' })`, updates the password through `updateUser({ password })`, then signs out and returns to Log In. The Supabase Password Recovery template must include `{{ .Token }}`. |
| DEC-056 | 2026-05-09 | Separate barangay verification from public profile completion before posting or messaging. | Accepted | Verified users can still look untrustworthy if their public profile is empty. Forcing full profile setup during onboarding would make entry too heavy, so the safer MVP gate is action-based completion after verification. | Profile tab becomes a trust center with Core, Work, and Hiring completion. Job publishing and worker messaging require Hiring Profile. Service publishing and client messaging require Work Profile. Verification files remain private and are never reused as public profile content. |
| DEC-057 | 2026-05-09 | Keep auth/onboarding screens mounted during status refresh and block duplicate signup before OTP. | Accepted | Re-showing the app splash after OTP/password/profile transitions looks like an app restart during demos. Sending signup OTPs to existing accounts also wastes email quota and creates confusing duplicate-account paths. | Root navigation only shows the splash during initial app boot. Later auth/profile status reloads keep the current screen visible while routing settles. `signup-email-check` blocks completed profile/auth accounts before OTP send while still allowing in-progress signup-password flows. |
| DEC-058 | 2026-05-13 | Keep adviser refinements focused on privacy-safe barangay marketplace setup. | Accepted | Adviser feedback clarified that Konektado needs better address privacy, setup gating, rate ranges, taxonomy discipline, and moderated trust states before adding bigger integrations. | Store split address fields and private job notes, show only approximate location publicly, separate official taxonomy values from custom "Others / Specify" text, add job/service rate ranges and experience/certification metadata, hide own posts from discovery, keep reviews controlled after completed interactions, and treat `Verified · Setup incomplete` as a browse-only state. Barangay/admin handles verification/moderation now; barangay/LGU system integration remains future scope. |
| DEC-059 | 2026-05-14 | Make provider onboarding service selection progressive and store work setup separately. | Accepted | Showing all service groups and chips at once makes first onboarding feel heavy, especially now that Konektado supports on-site and online work. Work setup is a matching/filtering dimension, not an official service value. | Provider onboarding now uses selector rows and bottom sheets for work setup, categories, and services. `user_preferences.offered_delivery_mode` stores `on_site`, `online`, or `both`; official taxonomy services stay in `offered_services`, and custom service text stays in `custom_offered_services` with pending review where provider profile status exists. |
| DEC-060 | 2026-05-14 | Treat rate ranges as the canonical marketplace pricing model and keep auth users out of demo cleanup. | Accepted | The prototype still had old single-price demo content and some display paths that rendered `PHP` text, even though the schema already supported ranges. Demo cleanup should repair app data without wiping login accounts. | Jobs use `budget_min`/`budget_max`, services and Work Profiles use `rate_min`/`rate_max`, shared formatting renders peso ranges, search uses range overlap, and cleanup/backfill SQL stays outside migrations until explicitly approved. Auth users must not be deleted for remote cleanup. |
| DEC-061 | 2026-05-15 | Use one shared identity with separate Work and Hiring profile readiness. | Accepted | Konektado users may only offer services or only hire help, so showing one side as `0% complete` is punitive and inaccurate. Barangay verification is official trust state, while credentials are optional supporting proof. | Shared identity stays in `profiles`; Work and Hiring completion are calculated independently. Untouched modes show `Not set up`. Approved users are not asked to submit verification again. Credentials use optional private metadata/storage and do not block completion, publishing, or matching. Service/job publishing requires numeric min/max rate or budget ranges with negotiability as a separate flag. |
| DEC-062 | 2026-05-15 | Strongly recommend public profile photos without making them a gate. | Accepted | A clear public photo helps neighbors recognize one another, but forcing one would make a trust booster feel like a second verification requirement. | Shared identity now supports optional public avatar uploads from a dedicated `profile-photos` bucket. Missing photos appear as optional Profile Builder and Profile hero prompts, while completion, verification, publishing, messaging, and matching remain unchanged. Private verification files must never become public avatars. |
| DEC-063 | 2026-05-15 | Use inline guidance before action, toast/snackbar feedback after routine success, and modal alerts only when interruption is necessary. | Accepted | Blocking success alerts made ordinary flows feel prototype-like and slowed onboarding/profile work without adding safety. | Konektado now has a shared app-wide feedback host. Routine success states such as profile saves, credential adds, service saves, job posts, password reset completion, and report submission use snackbars; modals remain for destructive confirmation, legal/privacy choices, serious acknowledgments, and verification/setup gates. |
| DEC-064 | 2026-05-15 | Make remediation prompts land on the exact field or section that resolves them. | Accepted | When Konektado already knows what is missing, sending the user only to the top of a long form adds needless search work and makes Profile Builder feel less intelligent than it is. | Profile Builder actions now carry focus targets into profile, service, and job editors. Those screens scroll directly to profile photo, shared identity, contact preference, service area, availability, rate range, hiring intro, needed services, preferred schedule, service rate, or job budget sections as appropriate. |
| DEC-065 | 2026-05-16 | Match client onboarding to the provider selector-row pattern without persisting help setup. | Accepted | The client service chip wall felt visually different from the provider onboarding flow. Clients can still use on-site, online, or both as a temporary narrowing choice, but the product does not need a persisted `needed_delivery_mode` preference yet. | Client onboarding now uses selector rows and bottom sheets for help setup, categories, services, and optional other help. Help setup only filters visible options and prunes invalid temporary selections; persisted onboarding data remains `needed_services` and `custom_needed_services`. |
| DEC-066 | 2026-05-17 | Add a basic in-app notification center while keeping push delivery out of MVP. | Accepted | The Home and Messages bells should point to real product behavior, but push notifications, background jobs, and preference systems would widen the slice without helping the current demo path. | Konektado now stores owner-scoped notification rows for new messages, verification decisions, completed hired jobs, and report status updates. Home and Messages open one shared center with unread/read state, while push delivery and notification preferences remain future work. |
| DEC-067 | 2026-05-17 | Give Home its own lightweight discovery filter instead of routing every feed-filter action into Search. | Accepted | Home and Search serve different jobs: Home is for passive discovery, while Search is for intentional lookup. A small Home filter preserves that distinction without duplicating the advanced Search sheet. | Home now filters its existing bounded discovery feed locally by feed type, work mode, MVP category, text-based location preference, and sort, while Search remains the advanced route through a secondary `Advanced search` action. Nearby behavior stays honest because the MVP still lacks true geodistance. |
| DEC-068 | 2026-05-19 | Remove runtime dependence on legacy fixed-rate marketplace data. | Accepted | The schema supports rate/budget ranges, but old rows and seed data could still flow through fixed amount fallbacks. | Job/service display and search now use canonical `budget_min`/`budget_max` or `rate_min`/`rate_max` only. Negotiable remains a separate flag within a numeric range. Legacy fixed columns stay in the database for compatibility/backfill but are deprecated and not used by app services. |
| DEC-069 | 2026-05-19 | Preserve auth while reseeding presentation marketplace content. | Accepted | Prototype walkthroughs need a populated, believable Barangay San Pedro marketplace without deleting login accounts, auth identities, verification documents, credential files, migrations, or storage bucket configuration. | `supabase/reseed_presentation_content_preserve_auth.sql` selects existing non-admin sample accounts, fails if fewer than 6 are available, refreshes only visible profile and role-profile display fields, clears old marketplace/content rows, regenerates presentation-safe conversations and messages, and inserts fictional sample services/jobs with range pricing for system demonstration. This content must be replaced before official launch. |
| DEC-070 | 2026-05-26 | Use one Figma-aligned Barangay Admin shell with bottom admin navigation. | Accepted | The admin verification, report, user review, public-photo review, and settings surfaces should feel like one official barangay module instead of unrelated pages. The latest Figma admin nodes establish a compact blue brand header, white page header, underline filters, minimal list surfaces, and fixed admin bottom navigation. | Admin routes share `/components/admin/AdminShell.tsx` primitives for header, page title, filter tabs, metrics, empty/error/loading states, status badges, info rows, list cards, and bottom nav. Bottom admin navigation uses Verifications, Reports, Users, and Settings. Photos remains a secondary public-facing photo review route and must never show verification documents, government IDs, private certificates, or signed verification files. |
| DEC-071 | 2026-05-26 | Add backend-backed moderation for public-facing photos without storage deletion. | Accepted | Barangay admins need real Flag, Hide, and Clear actions for profile avatars, job photos, and service photos, but account restriction and physical storage takedown need a broader enforcement design. | `admin_moderation_actions` stores admin audit history, `content_visibility` stores public UI visibility, and public app services filter hidden photos before rendering. Admin notes/reasons stay admin-only. Verification documents, government IDs, certificates, credential files, private storage files, and signed verification URLs remain outside Photos and Users. |
| DEC-072 | 2026-05-27 | Enforce verified-owner public discovery and realistic demo profile photos. | Accepted | Home/Search are public marketplace discovery surfaces, so demo rows from pending or rejected users should not appear just because a seed row is active. The thesis demo also needs realistic public profile photos instead of cartoon/avatar generators. | `searchJobs` and `searchServices` always filter public discovery to verified owners after photo visibility filtering. Admin user/photo review uses the same canonical pending-first verification status and the public avatar source is `profiles.avatar_url`. Seed/reseed scripts use realistic stock-style profile photos, keep private verification files separate, assign active public content only from verified owner pools, and fail validation if unverified users receive active public jobs/services or public photo URLs point to cartoon/avatar, ID, certificate, credential, or verification-file assets. |
| DEC-073 | 2026-05-27 | Add a hidden internal demo content editor route for development-team data curation only. | Accepted | The team needs a safer way to curate realistic presentation data without editing Supabase tables directly, but this must not become an official Konektado feature or barangay-admin demo surface. | `/internal/demo-editor` is a direct-URL internal web-capable route guarded by admin access plus optional email/user-id whitelist env vars. It must not be linked from public tabs or admin bottom navigation. It uses whitelisted service-layer updates, public photo buckets for public images, and short-lived signed links for private verification documents. Creation for other users and private document replacement remain deferred until a dedicated internal RPC/storage policy design exists. |
| DEC-074 | 2026-05-27 | Make presentation seed public activity verified-only across jobs, services, conversations, reviews, and public work photos. | Accepted | Internal review found rejected/unverified accounts can look like normal marketplace participants if old rows survive or if validation only checks active posts. This breaks the verification-gated product story during demos. | Seed and preserve-auth reseed validation now fails when pending/rejected/unverified users have active jobs, active services, normal work conversations, completed reviews, or job/service public photos. Visible presentation copy must avoid seed/demo/test/internal wording, public image URLs must avoid cartoon/private sources and duplicate URL usage, and a manual verification SQL helper is available after reseed. |
| DEC-075 | 2026-05-27 | Allow the hidden internal demo editor to create demo jobs and services through admin-only RPCs. | Accepted | Direct client inserts must keep normal RLS locked to owner-created marketplace content, but the development team needs a controlled way to repair and prepare presentation data for selected residents. | `/internal/demo-editor` can create jobs and services for selected residents only through `internal_demo_create_job` and `internal_demo_create_service`, both gated by `public.is_barangay_admin()`. Active public jobs/services remain blocked for unverified, pending, or rejected users. Private verification file add/replace is allowed only through admin-gated storage/table policies and files stay in the private verification bucket. |
| DEC-076 | 2026-05-27 | Save internal demo jobs for unverified residents as private drafts instead of public posts. | Accepted | Admins preparing demo data can accidentally create public marketplace rows for residents whose barangay verification is still pending, rejected, or missing. The safer internal workflow is to preserve the entered data privately and keep public discovery verified-only. | `/internal/demo-editor` routes new jobs for unverified residents through `internal_demo_create_job_draft`, shows existing private drafts for audit context, and forces new or edited services for unverified residents to remain inactive. Conversation/report status controls and public-photo Hide/Flag/Clear actions are available for demo cleanup, while messages, reviews, and private verification files stay content-read-only except for explicit verification-file replacement. |
| DEC-077 | 2026-05-28 | Lock verified/legal names after barangay verification submission and use Needs Correction for name mismatches. | Accepted | The resident's verified/legal name is part of Konektado's trust model, so users must not silently change identity after submission. Honest typos, nicknames, or ID mismatches need a correction path that does not imply fraud. | Normal profile and verification services allow name edits only before submission or when the latest verification is `needs_more_info` with a name-correction reason. A profile database trigger blocks direct `first_name`, `last_name`, and `full_name` changes after submission except controlled name correction or admin override. Admin review now distinguishes Approve, Needs Correction, and Reject, with `Name does not match submitted ID` enabling name correction during resubmission. |
| DEC-078 | 2026-05-28 | Route public marketplace profile reads through a safe profile summary RPC. | Accepted | Broad authenticated reads of verified `profiles` rows could expose private columns if a client queried them directly. Public Home, Search, profile, job, service, review, and report surfaces need only safe identity fields and a general nearby signal. | Public profile summaries now use `get_public_profile_summaries`, which returns safe public fields plus a computed `public_location_label` only. Raw street, subdivision/area, and purok/sitio fields are not returned by the public summary RPC. Full `profiles` row reads remain for owners, barangay admins, and guarded internal tooling. The previous broad verified-profile select policy is removed after the public read path is migrated. |
| DEC-079 | 2026-05-31 | Keep the hidden internal demo editor aligned with the marketplace fields rendered by public discovery. | Accepted | The editor could create and update core job/service rows, but it left showcase-critical fields such as schedules, tags, availability, experience, certification, negotiability, and listing options at hardcoded defaults. It also coupled barangay filtering data to descriptive public location text. | `/internal/demo-editor` now curates the existing rich job/service columns used by Home, Search, and detail/profile screens, keeps barangay separate from public location text, rejects category/service mismatches, and confirms listing deactivation. Admin-only create RPCs persist the same fields without widening permissions or exposing private verification files. |

## Active Debugging Log

| Date       | Time                | Topic                                   | Status           | Notes                                                                                                                                                                                                                                                                                                                                                                                                      |
| ---------- | ------------------- | --------------------------------------- | ---------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 2026-05-03 | 00:00 Asia/Shanghai | OTP verification on real Android device | Root cause fixed | Supabase Auth was configured to generate 8-digit OTP codes while the app input and email template displayed only 6 digits. The Supabase setting has been changed to 6 digits. The auth service has been simplified back to the steady-state flow: `signInWithOtp({ shouldCreateUser: true })`, resend with `signInWithOtp()`, verify with `verifyOtp({ type: 'email' })`, then `updateUser({ password })`. |

## How to Add Decisions

When a major decision changes scope, architecture, data model, security, or delivery priorities, add a new row:

| ID      | Date       | Decision            | Status                     | Rationale                   | Consequences                       |
| ------- | ---------- | ------------------- | -------------------------- | --------------------------- | ---------------------------------- |
| DEC-XXX | YYYY-MM-DD | Decision statement. | Proposed/Accepted/Replaced | Why this decision was made. | What this changes for the project. |
